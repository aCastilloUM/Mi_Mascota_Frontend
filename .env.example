# Copy this file to `.env` and set your Google Maps API key to enable Google Places.
# Do NOT commit your real API key to version control.
# To get a key: https://console.cloud.google.com/apis/credentials — enable Maps JavaScript API and Places API, attach billing (Google provides a monthly free credit).

VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
